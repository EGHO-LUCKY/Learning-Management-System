$kind: http-request
url: "{{baseUrl}}/api/v1/admin/coupons/:couponId"
method: PUT
headers:
  Content-Type: application/json
pathVariables:
  couponId: "{{couponId}}"
body:
  type: json
  content: "{}"
order: 10100
