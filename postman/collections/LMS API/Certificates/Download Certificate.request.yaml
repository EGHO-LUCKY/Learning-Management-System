$kind: http-request
name: 'Download Certificate'
method: GET
url: '{{baseUrl}}/api/v1/certificates/:certificateId/download'
order: 5000
pathVariables:
  - key: certificateId
    value: '{{certificateId}}'
