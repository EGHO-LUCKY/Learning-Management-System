$kind: http-request
method: POST
url: '{{baseUrl}}/api/v1/coupons/validate'
order: 8000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "code": "SAVE20",
      "courseId": "{{courseId}}"
    }
